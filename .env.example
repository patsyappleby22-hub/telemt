# Local development — copy to .env and fill in values
POSTGRES_PASSWORD=changeme

# For panel (when running server directly, not via docker-compose)
DATABASE_URL=postgres://telemt:changeme@localhost:5432/telemt
DATABASE_SSL=false
PORT=9092
